server:
  port: 8083

eureka:
  instance:
    hostname: localhost


spring:
  cloud:
    gateway:
      routes:
        - id: backend
          uri: lb://customer-service
          predicates:
            - Path=/user/**
        - id: AuthenticationService
          uri: lb://AuthenticationService
          predicates:
            - Path=/auth/**
        - id: CONTACT-SERVICE
          uri: lb://CONTACT-SERVICE
          predicates:
            - Path=/contact/**
  application:
    name: API-GATEWAY